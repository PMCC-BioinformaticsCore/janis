inputs:
  fastq:
    SequenceReadArchivePaired:
      forward-pattern: '*_R1.fastq.gz'
      backward-pattern: '*_R2.fastq.gz'
  ref:
    BAM:
      path: 'path/to/reference'

steps:
  - step1:
      trim:
        trimmer : 'trimmomatic'
  - step2:
      input_from: ['ref']
      align:
        aligner: 'bwa'
  - step3:
      dedup:
  - step3:
      input_from: ['step2', 'step3']
      call:
